import { Component, EventEmitter, Input, OnInit, Output } from '@angular/core';
import { FormGroup } from '@angular/forms';

@Component({
    templateUrl: './dli.html',
    selector: 'dli-display',
})
export class DliDisplay implements OnInit {
    constructor() {}
    ngOnInit(): void {}
}
