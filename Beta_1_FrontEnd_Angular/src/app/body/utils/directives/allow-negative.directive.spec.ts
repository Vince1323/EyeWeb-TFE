import { AllowNegativeDirective } from './allow-negative.directive';

describe('AllowNegativeDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowNegativeDirective();
    expect(directive).toBeTruthy();
  });
});
